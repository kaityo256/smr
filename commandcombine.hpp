#pragma once
#include <vector>
#include <string>
#include <unordered_map>

int commandCombine(const std::vector<std::string> &positional, const std::unordered_map<std::string, std::string> &arguments);
